.container-fluid(style="min-height: 400px;")
  .fullscreen-wrapper(title="{{image.name}}")
  .page-header
    h1
      | {{image.name}}
    img.img-responsive(ng-src="/images/gallery/full/{{image.original.name}}", width="100%", height="100%")
    p(ng-bind-html="image.description")
