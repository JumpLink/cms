/**
 * 
 */
module.exports = function (grunt) {
	grunt.registerTask('build', [
		// 'hub:clean',
		// 'hub:build-dev'
	]);
  grunt.registerTask('prod', [
    // 'hub:clean',
    // 'hub:build-prod'
  ]);
};
